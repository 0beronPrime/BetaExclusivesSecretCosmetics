<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cosmetic Request</title>
</head>
<body>

    <h2>Claim Your Cosmetics</h2>

    <form id="cosmetic-form">
        <label for="unique-code">Enter Your Unique Code:</label>
        <input type="text" id="unique-code" name="unique-code" required>

        <label for="email">Enter Your Email:</label>
        <input type="email" id="email" name="email" required>

        <button type="submit">Claim</button>
    </form>

    <script>
        document.getElementById('cosmetic-form').addEventListener('submit', function (event) {
            event.preventDefault();
            const uniqueCode = document.getElementById('unique-code').value;
            const email = document.getElementById('email').value;
            
            // TODO: Envoyer les donn̩es au backend pour v̩rification et distribution.
        });
    </script>

</body>
</html>
